import 'package:get/get.dart';

showSnackbar({String? title, message}) {
  Get.showSnackbar(
    GetSnackBar(
      title: title ?? "Ups Something Error from Response API",
      message: message,
      duration: const Duration(seconds: 3),
    ),
  );
}